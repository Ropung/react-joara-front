export default interface bannerProps {
  url: string;
  alt: string;
  bannerId: string;
}

export default interface novelsProps {
  url: string;
  alt: string;
  novelId: string;
  category: string;
  tag: string;
}
